// Fonction pour créer la fenêtre modale
function createModal() {
    // Code pour créer et afficher la fenêtre modale
    // ...
  }
  
  // Écouteur d'événement pour ouvrir la fenêtre modale au clic sur le bouton "Modifier"
  const modifierButton = document.querySelector('.modifier-button');
  modifierButton.addEventListener('click', createModal);
  
  // Fonction pour fermer la fenêtre modale
  function closeModal() {
    // Code pour fermer la fenêtre modale
    // ...
  }
  
  // Écouteur d'événement pour fermer la fenêtre modale au clic sur la croix
  const closeButton = /* Sélectionne la croix dans la fenêtre modale */;
  closeButton.addEventListener('click', closeModal);
  
  // Fonction pour gérer l'ajout de photo
  function addPhoto() {
    // Code pour ajouter une photo à la galerie
    // ...
  }
  
  // Écouteur d'événement pour ajouter une photo dans la fenêtre modale
  const addPhotoButton = /* Sélectionne le bouton "Ajouter une photo" */;
  addPhotoButton.addEventListener('click', addPhoto);